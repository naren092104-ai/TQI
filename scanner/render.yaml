services:
  - type: web
    name: tqi-bill-scanner
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --workers 2 --timeout 120 --bind 0.0.0.0:$PORT
    plan: starter
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
